<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools" android:id="@+id/last_post_content"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:orientation="vertical"
	style="@style/HomeCardContent"
	android:background="?selectableItemBackground"
	android:foreground="?android:attr/selectableItemBackground"
	android:clickable="true"
	android:focusable="true">

	<TextView
		android:id="@+id/date"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:paddingBottom="12dp"
		app:layout_constraintTop_toTopOf="parent"
		app:layout_constraintStart_toStartOf="parent"
		app:layout_constraintEnd_toEndOf="parent"
		android:textAppearance="@style/TextAppearance.AppCompat.Caption"
		android:textSize="14sp"/>

	<!-- paddingEnd - это костыль, пока не работает marginEnd -->
	<!-- width=80 - это костыль, ппотому что padding обрезает изображение -->
	<com.makeramen.roundedimageview.RoundedImageView
		android:id="@+id/preview"
		android:layout_width="80dp"
		android:layout_height="72dp"
		app:layout_constraintTop_toBottomOf="@id/date"
		app:layout_constraintStart_toStartOf="parent"
		android:paddingEnd="8dp"
		android:scaleType="fitCenter"
		app:riv_corner_radius="4dip"
		android:visibility="gone"
		tools:ignore="RtlSymmetry"/>

	<TextView
		android:id="@+id/title"
		android:layout_width="0dp"
		android:layout_height="wrap_content"
		app:layout_constraintTop_toBottomOf="@id/date"
		app:layout_constraintStart_toEndOf="@id/preview"
		app:layout_constraintEnd_toEndOf="parent"
		android:textAppearance="@style/TextAppearance.AppCompat.Title"
		android:textSize="16sp"/>

</androidx.constraintlayout.widget.ConstraintLayout>